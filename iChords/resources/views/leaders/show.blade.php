@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-5xl px-5 py-10 sm:px-8 lg:px-12"><a href="{{ route('home') }}" class="back-link">← All leaders</a><div class="mt-14 flex flex-col justify-between gap-8 sm:flex-row sm:items-end"><div><p class="eyebrow">Song lineup / 02</p><h1 class="serif mt-3 text-5xl tracking-tight sm:text-6xl">{{ $leader['name'] }}</h1><p class="mt-3 text-stone-500">{{ $leader['role'] }} · a personal set of favorites</p></div><a href="{{ route('songs.create', $leader['slug']) }}" class="button-primary">＋ Add song</a></div><div class="mt-14 flex items-center justify-between border-b border-stone-200 pb-4 dark:border-stone-700"><div class="search-wrap"><span>⌕</span><input data-song-search data-url="{{ route('leaders.search', $leader['slug']) }}" placeholder="Search this lineup..." autocomplete="off"><button data-search-clear hidden aria-label="Clear search">×</button></div><span data-search-status class="mono text-xs text-stone-400">{{ count($songs) }} songs</span></div><div data-song-results class="mt-2">@foreach($songs as $song)<a href="{{ route('songs.show', $song['slug']) }}" class="song-row group"><span><span class="song-title">{{ $song['title'] }}</span><span class="song-meta">{{ $song['artist'] }} · Key {{ $song['key'] }}</span></span><span class="row-arrow">↗</span></a>@endforeach</div></div>
@endsection
