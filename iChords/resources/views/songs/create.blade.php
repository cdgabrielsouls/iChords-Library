@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-3xl px-5 py-10 sm:px-8 lg:px-12"><a href="{{ route('leaders.show', $leader['slug']) }}" class="back-link">← {{ $leader['name'] }}'s lineup</a><div class="mt-14"><p class="eyebrow">New song / 04</p><h1 class="serif mt-3 text-5xl tracking-tight">Add to the shelf.</h1><p class="mt-4 max-w-xl text-stone-500">Add the song details and paste the lyrics first. After saving, you can place chords above each word.</p></div><form method="POST" action="{{ route('songs.store', $leader['slug']) }}" class="mt-12 space-y-7">@csrf<div class="grid gap-7 sm:grid-cols-2"><label class="field"><span>Song title</span><input name="title" required placeholder="e.g. Great Are You Lord" value="{{ old('title') }}">@error('title')<small>{{ $message }}</small>@enderror</label><label class="field"><span>Artist <i>optional</i></span><input name="artist" placeholder="Original artist" value="{{ old('artist') }}"></label><label class="field"><span>Original key</span><input name="original_key" placeholder="e.g. G" value="{{ old('original_key') }}"></label></div><label class="field"><span>Lyrics only</span><textarea name="lyrics_chords" required rows="12" placeholder="How great is our God, sing with me how\nGreat is our God\nAnd all will see">{{ old('lyrics_chords') }}</textarea><small>Paste lyrics only. You will add the chords in the next step.</small></label><label class="field"><span>Notes <i>optional</i></span><textarea name="notes" rows="3" placeholder="Arrangement notes, capo, or reminders">{{ old('notes') }}</textarea></label><div class="flex items-center justify-between border-t border-stone-200 pt-7 dark:border-stone-700"><a href="{{ route('leaders.show', $leader['slug']) }}" class="text-sm font-semibold text-stone-500">Cancel</a><button class="button-primary" type="submit">Save lyrics <span>↗</span></button></div></form></div>
@endsection
